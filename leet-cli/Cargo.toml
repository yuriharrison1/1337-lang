[package]
name = "leet-cli"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
authors.workspace = true
license.workspace = true
repository.workspace = true
homepage.workspace = true
description = "Command-line tools for debugging, inspecting, and benchmarking 1337"
keywords.workspace = true
categories.workspace = true
documentation = "https://docs.rs/leet-cli"
readme = "README.md"

[[bin]]
name = "leet"
path = "src/main.rs"

[dependencies]
leet-core = { path = "../leet-core", version = "0.5.1" }
leet-bridge = { path = "../leet-bridge", version = "0.5.1", features = ["keyword-fallback"] }
leet-mcp = { path = "../leet-mcp", version = "0.5.1" }
leet-service = { path = "../leet-service", version = "0.5.1" }
clap = { version = "4", features = ["derive"] }
clap_complete = "4"
crossterm = "0.27"
serde_json = "1"
colored = "2"
tokio = { version = "1", features = ["full"] }
uuid = { version = "1", features = ["v4"] }
reqwest = { version = "0.11", features = ["blocking"] }
indicatif = "0.17"

[dependencies.anyhow]
workspace = true

[dependencies.serde]
workspace = true
features = ["derive"]

[build-dependencies]
clap = { version = "4", features = ["derive"] }
clap_mangen = "0.2"

[dev-dependencies]
tempfile = "3"
